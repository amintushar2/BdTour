<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.raslab.bdtour">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2"
        android:usesCleartextTraffic="true">
        <activity android:name=".BusDetailsActivity"></activity>
        <activity android:name=".AirDetailsActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PopUpActivity" />
        <activity android:name=".TokenManager" />
        <activity android:name=".UserRegFrom" />
        <activity android:name=".Hotel_Room_rent_Details" />
        <activity android:name=".HotelListActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>